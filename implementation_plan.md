# خطة تنفيذ منصة إدارة وصيانة الأجهزة - مستشفى نيل الأمل (Nile of Hope Hospital IT Maintenance Platform)

تهدف هذه المنصة إلى تحويل عملية إدارة وصيانة أجهزة المستشفى من نماذج ورقية إلى نظام رقمي متكامل، حديث، وسهل الاستخدام يدعم اللغتين العربية والإنجليزية، ويوفر تحليلات دقيقة وتقارير فورية مطابقة للنموذج المعتمد.

---

## 1. المكونات الرئيسية للنظام (Core Modules)

1. **لوحة التحكم والإحصائيات الذكية (Analytics Dashboard):**
   - مؤشرات الأداء الرئيسية (Total Assets, Active Tickets, MTTR - Mean Time to Repair, Total Downtime, Maintenance Costs).
   - رسوم بيانية للأعطال حسب القسم (PICCU, Surgery, ICU, OPD, etc.)، نوع العطل (Hardware, Software, Network)، وحالة التذاكر.
   - تنبيهات الصيانة الوقائية والقطع منخفضة المخزون.

2. **سجل وإدارة الأجهزة (Asset Management):**
   - عرض قائمة الأجهزة مع تصفية متقدمة (حسب القسم، النوع، الحالة، الموقع).
   - بطاقة تعريفية لكل جهاز مع السجل الزمني الكامل للصيانات السابقة (Maintenance History Timeline).
   - توليد وطباعة ملصقات الـ **QR Code** مع كود الجهاز التسلسلي لسهولة المسح.
   - ماسح QR Scanner مدمج بالكاميرا لفتح الجهاز فوراً عند الفحص.

3. **إدارة تذاكر وبلاغات الصيانة (Maintenance Tickets & Work Orders):**
   - نموذج إنشاء تذكرة جديد ذكي وسلس (يستدعي بيانات الجهاز آلياً).
   - توثيق مراحل الصيانة: (البلاغ -> التشخيص والسبب الجذري -> الإجراء المتخذ -> قطع الغيار المستهلكة -> الحالة النهائية).
   - حساب مدة التوقف (Downtime) بدقة بين تاريخ البلاغ وتاريخ الانتهاء.
   - إمكانية التوقيع الإلكتروني لكل من الفني والمستخدم.

4. **تصدير وطباعة التقرير الرسمي (Official PDF Report Generator):**
   - زر لتوليد وطباعة تقرير صيانة رسمي ثنائي الصفحات مطابق تماماً للنموذج المعتمد لمستشفى نيل الأمل (Nile of Hope Hospital - IT Department - Maintenance Report) متضمناً الهيدر، الفوتر، الجداول، والتوقيعات.

5. **المخزون وقطع الغيار (Spare Parts & Inventory):**
   - قائمة قطع الغيار المتاحة، مصدر القطعة، الرصيد، والتكلفة.
   - خصم آلي للقطع المستخدمة في تقارير الصيانة وحساب التكلفة الإجمالية.

6. **الصيانة الوقائية والجدولة (Preventive Maintenance):**
   - جداول الفحص الدوري والتنظيف لأجهزة الأقسام الحيوية مع تذكيرات.

---

## 2. الهيكل التقني والملفات المقترحة (Proposed Technical Stack)

- **Frontend Framework:** React 18 + TypeScript + Vite (أداء عالي وسرعة فائقة).
- **Styling:** CSS3 حديث بنظام Design Tokens متكامل (متوافق مع Dark / Light Mode، دعم كامل للـ RTL العربي، تأثيرات Glassmorphism وتدرجات مستوحاة من القطاع الطبي التقني).
- **Icons & UI:** Lucide React icons.
- **Reporting & Export:** `jspdf` + `html2canvas` للطباعة وتصدير PDF الفوري، ومكتبة توليد `qrcode`.
- **Data Persistence:** LocalStorage مع طبقة Mock Data غنية مسبقة التحميل تحتوي على بيانات التقرير الأصلي وعدة أجهزة حقيقية بالمستشفى.

---

## 3. خطة العمل المتبعة (Implementation Steps)

### المرحلة 1: إعداد بيئة المشروع والمكتبات الأساسية
- إنشاء تطبيق Vite + React + TypeScript وتثبيت الحزم المطلوبة (`lucide-react`, `jspdf`, `html2canvas`, `qrcode.react`, etc.).
- تصميم وتأسيس نظام الألوان والمتغيرات والخطوط (Cairo / Inter) ودعم RTL/LTR.

### المرحلة 2: هيكلية البيانات والنماذج (Data Layer & State)
- تعريف أنواع البيانات (`Asset`, `MaintenanceTicket`, `SparePart`, `PreventiveSchedule`).
- تهيئة البيانات الافتراضية المأخوذة من التقرير (الجهاز `4th-PCCU-LPTP02`، التذكرة `R10-9-2026`، الفني `ENG Abdelrahman`، المستخدم `عبدالرحمن فتحي`).

### المرحلة 3: بناء المكونات والواجهات الرئيسية
- **الهيدر والشريط الجانبي والتنقل بين التبويبات.**
- **لوحة الإحصائيات (Dashboard) مع الرسوم البيانية التفاعلية.**
- **شاشة إدارة الأجهزة (Assets List & Details & QR Labels).**
- **شاشة تذاكر الصيانة (Tickets List & New Ticket Modal / Form).**
- **نافذة عرض وطباعة التقرير الرسمي (Print/PDF Template) المطابق لتقرير مستشفى نيل الأمل.**
- **شاشة المخزون وقطع الغيار (Inventory & Spare Parts).**
- **ماسح الـ QR التفاعلي (QR Scanner Tool).**

### المرحلة 4: التحقق والتجربة (Verification & Testing)
- تجربة إنشاء وتعديل تقارير الصيانة.
- فحص توافق الطباعة وتصدير الـ PDF.
- التأكد من استجابة الواجهة وسلاسة الأداء على جميع الشاشات.
